{
  "name": "patterns",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "css": "node-sass patterns.scss build/patterns.css",
    "watch": "watch 'npm run css' --wait=2 --ignoreDotFiles --ignoreDirectoryPattern=/build/",
    "serve": "serve",
    "dev": "npm-run-all --parallel watch serve"
  },
  "author": "Andrej Cremoznik",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.9.3",
    "npm-run-all": "^4.1.3",
    "serve": "^9.4.2",
    "watch": "^1.0.2"
  }
}
